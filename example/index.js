import { registerRootComponent } from 'expo'

import App from './App'

// Expo's `registerRootComponent` calls AppRegistry.registerComponent('main', ...)
// and on web also wires the module up to render to a root DOM node.
registerRootComponent(App)
